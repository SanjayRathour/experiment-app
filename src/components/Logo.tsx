import { Box } from "@mui/system";
import React from "react";

const Logo = () => {
  return (
    <Box sx={{ textAlign: "right" }}>
      <img src="/logo/logo.png" width={130} height={130} alt="logo" />
    </Box>
  );
};

export default Logo;
