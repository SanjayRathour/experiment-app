import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  direction: "rtl",
  palette: {
    primary: {
      main: "#6E78FF",
    },
    white: "#ffffff",
    black: "#868D96",
  },

  spacing: 4,
});

export default theme;
